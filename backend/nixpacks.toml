[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "echo 'No build needed'"

[start]
cmd = "npm start"

[variables]
NIXPACKS_NO_INSTALL_CACHE = "1"